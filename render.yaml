services:
  - type: web
    name: robot-price-monitor
    runtime: static
    rootDir: 09_DASHBOARDS/robot-price-monitor
    buildCommand: echo "No build needed"
    staticPublishPath: .
    autoDeployTrigger: commit
